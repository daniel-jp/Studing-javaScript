/*
const name = 'Daniel'
console.log(`Hi ${name}`)

const sentece = `This is really a conenience, 
          You probably will never 
          want to do samething
        quite like this. 
      You might chose
`
console.log(sentece)
*/
