//variables
var a = 2 //VAR variable: It can be accessed without initialization as its default value is “undefined”.

a = 10 // he get the last value//
const b = 4 //CONST variable:: It cannot be accessed without initialization, as it cannot be declared without initialization.

let c = 55 //LET variable : It cannot be accessed without initialization otherwise it will give 'referenceError'.
var sum
sum = a + b + c

console.log('Sum : ' + sum)
